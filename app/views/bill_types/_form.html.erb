<%= form_with(model: bill_type, local: true) do |form| %>
  <% if bill_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_type.errors.count, "error") %> prohibited this bill_type from being saved:</h2>

      <ul>
      <% bill_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
